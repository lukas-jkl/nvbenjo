defaults:
  - default
  - _self_

nvbenjo:
  enable: true
  measure_memory: true
  torch_profile: false
  models:
  - name: dummytorchmodel
    type_or_path: replaceme
    kwargs: {}
    shape: [{"name": "x", "shape": ["B", 10], "dtype": "float", min_max: [0, 0.5]}]
    num_warmup_batches: 1
    num_batches: 2
    batch_sizes:
    - 1
    devices:
    - "cpu"
    precisions:
    - FP32